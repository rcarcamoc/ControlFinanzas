package com.aranthalion.controlfinanzas.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta de colores basada en el prototipo FinaVision
// Tonos naranja/terracota para mantener consistencia visual

// Colores principales
val OrangePrimary = Color(0xFFE67E22) // Vibrant Orange (primary)
val OrangeSecondary = Color(0xFFF39C12) // Lighter Orange (secondary)
val OrangeAccent = Color(0xFFD35400) // Darker Orange (accent)

// Colores de fondo
val BackgroundLight = Color(0xFFFEF9F6) // Very Light Orange (Pale Peach)
val BackgroundDark = Color(0xFF2C1810) // Very Dark Brownish Orange

// Colores de superficie
val SurfaceLight = Color(0xFFFFFFFF) // White cards for contrast
val SurfaceDark = Color(0xFF3C2818) // Slightly lighter dark brownish orange for cards

// Colores de texto
val TextPrimaryLight = Color(0xFF4A2F1A) // Dark Brownish Orange for text
val TextPrimaryDark = Color(0xFFF5E6D3) // Light Peachy Orange for text

// Colores de texto secundario
val TextSecondaryLight = Color(0xFF7A5A3A) // Darker peachy brown for muted text
val TextSecondaryDark = Color(0xFFD4B08C) // Lighter Peachy Orange for muted text

// Colores de borde
val BorderLight = Color(0xFFE8D5C4) // Soft orange border
val BorderDark = Color(0xFF4A3A2A) // Muted orange-brown border

// Colores de entrada
val InputLight = Color(0xFFF0E6DC) // Slightly darker soft orange for input
val InputDark = Color(0xFF4A3A2A) // Muted orange-brown for input

// Colores de estado
val Success = Color(0xFF27AE60) // Green for success
val Error = Color(0xFFE74C3C) // Red for error
val Warning = Color(0xFFF39C12) // Orange for warning

// Colores de gráficos (para charts)
val Chart1 = Color(0xFFE67E22) // Vibrant Orange
val Chart2 = Color(0xFFF39C12) // Lighter/Softer Orange - Peach
val Chart3 = Color(0xFFD35400) // Complementary Brownish Orange
val Chart4 = Color(0xFFF8C471) // Lighter Vibrant Orange
val Chart5 = Color(0xFFE67E22) // Mid Orange/Terracotta

// Colores legacy para compatibilidad
val Purple80 = OrangePrimary
val PurpleGrey80 = OrangeSecondary
val Pink80 = OrangeAccent

val Purple40 = OrangeAccent
val PurpleGrey40 = OrangeSecondary
val Pink40 = OrangePrimary